<div id="header">
    <a href="/"><img src="/images/todo.png"></a><h4 class="brand-title">&nbsp;next gen todo app</h4>
    <span class="sign-buttons">
      {{#if user}}
      Welcome {{user.email}} &nbsp;
      <a href='/todo' class="btn btn-default">my todo</a>
      <a href="/logout" class="btn btn-danger">Logout</a>
      {{else}}
      <a href='/login' class="btn btn-default">Login</a>
      <a href='/signup' class="btn btn-default">Signup</a>
    </span>
    {{/if}}
</div>